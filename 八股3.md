# 42. 野指针和悬空指针的概念？

| 类型         | 定义                                 | 原因及危害                                                   |
| ------------ | ------------------------------------ | ------------------------------------------------------------ |
| **野指针**   | 指针变量指向一个**随机的地址**       | 通常是因为没有初始化指针变量。使用野指针可能会读取到不属于程序的内存区域，导致数据损坏或程序崩溃 |
| **悬空指针** | 指向了一块**已经被释放的内存的指针** | 如果在释放地址后没有将指针设置为`nullptr`，再次使用这个指针可能会发生未定义的行为，如访问已释放的内存。 |

​	**(1)野指针：**野指针则是**指向不确定内存区域的指针**。这通常是因为**指针变量未被初始化就开始使用**，或是**指向的内存已被释放**（比如用 `free` 或 `delete` 归还给系统）或**重新用于其他用途**后（比如同一个地址被新的变量占用），**该指针未被立即清零或重新赋值**，它仍然傻傻地保存着原来的地址值。此时，这个指针就成了“野针”——它指向的是一块“**已经不再属于你**”或者“**用途已变**”的内存。

野指针的危害在于它可能指向任何位置，对其的任何操作都可能导致不可预测的结果，如程序崩溃或数据损坏。

“野指针”被当作一个 **更宽泛的概念**——**任何指向无效（非法、不可预知）内存区域的指针**，都叫野指针。这样分类时：

- **未初始化的指针**（狭义野指针） → 指向“绝对随机”的地址。
- **已释放未置空的指针**（**悬空指针**） → 指向“曾经合法但当前已不合法”的地址。

​	**(2)悬空指针：**悬空指针是**指向一块已经被释放的内存的指针**。这通常发生在对象或内存块被删除后，指向该内存的指针没有被及时设为 `nullptr`。这种指针的危险在于，如果继续使用它，可能会操作一些已被操作系统回收或重新分配给其他程序的内存，从而导致数据损坏或程序异常。

# 43. 内存对齐？

 ==[1存储.md](..\刷题\牛客C++\1存储.md) ---》类型字节对齐方式==

​	表：**内存对齐**

| 优化方面                 | 描述                                                         |
| ------------------------ | ------------------------------------------------------------ |
| **提高内存访问速度**     | 对齐的数据**允许 CPU 以最快的方式访问内存**。数据对齐到特定的内存地址，使得 **CPU 可以在单个或最少的内存访问周期内完成数据读写**。 |
| **防止硬件错误**         | 某些处理器（如 ARM、SPARC）对**未对齐的数据访问可能引发硬件异常**。内存对齐避免这类问题，确保程序的稳定运行。 |
| **遵守硬件和编译器约定** | 大多数现代编译器和 CPU 架构都有内存对齐的要求。遵循这些约定可以优化代码的执行，提高性能。 |
| **平台兼容性**           | 在**不同平台之间共享数据**时，正确的内存对齐确保**数据结构的一致性**和**正确解析**，特别是在**网络通信和文件格式**标准中。 |
| **提高缓存利用率**       | 正确对齐的数据结构更可能与缓存行边界对齐，减少缓存行加载次数，提高缓存利用率，从而**减少 CPU 缓存未命中的情况**。 |

**内存对齐：**一种计算机系统中用于安排数据存储和访问的技术，以**提高内存访问的效率**。

- **提高性能：**内存对齐使得数据存放在处理器预期的内存地址上，减少对内存的访问次数，从而提高处理速度。

- **防止错误：**某些处理器要求数据严格对齐，否则可能导致运行时错误。
- **兼容性：**正确的内存对齐保证了数据结构在不同计算机系统之间能够正确读取和写入，尤其是在涉及网络通信和文件存储的应用中。

````c++
struct Aligned {
int a;
int b;
int c;
}; // 自然对齐，通常按照最大成员（这里是 int，假设 4 字节）对齐
#pragma pack(push, 1)
struct Unaligned {
char x; // 1 字节
int a; // 4 字节，应对齐至 4 字节边界
char y; // 1 字节
int b; // 4 字节，应对齐至 4 字节边界
char z; // 1 字节
int c; // 4 字节，应对齐至 4 字节边界
};
````

`#pragma pack(pop)` 、`#pragma pack()`、`#pragma pack(push, n)`

 ==见： [1存储.md](..\刷题\牛客C++\1存储.md) ---》`#pragma pack()` // 重置为 编译器默认对齐==

![内存对齐](D:\Typora\typora_work\C++规划\内存对齐.png)

# 44. extern关键字

 ==[引用.md](..\刷题\牛客C++\引用.md)== 



# 61. 