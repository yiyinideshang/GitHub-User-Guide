# **第二章 c++11常用新特性**

# 学习C++新特性：

- 卡码笔记：https://notes.kamacoder.com/cpp/cpp11-new-features.html
- C++在线参考手册：https://cppreference.com/
- GitHub：[C++那些事 ](https://github.com/Light-City/CPlusPlusThings?tab=readme-ov-file)

![C++新特性](D:\Typora\typora_work\C++规划\C++新特性.png)

# 1.自动类型推导 (`auto`/`decltype`)

**Q1：auto 和 decltype 有什么区别？**

用 auto 让编译器自动**推导变量类型**。

用 decltype 获取**表达式的类型**。

```c++
auto x = 42;              // int
decltype(x + 3.14) y = 0; // double
```

## 关于`typeid`

`typeid(x).name()` 获取类型的内部名称，输出结果是 **名称修饰（name mangling）** 后的字符串。根据常见编译器（GCC / Clang）的 ABI 规则，含义如下：

| 输出  | 对应的实际类型 | 解释                                                         |
| :---- | :------------- | :----------------------------------------------------------- |
| `i`   | `int`          | `i` 是 `int` 的简单修饰名                                    |
| `d`   | `double`       | `d` 是 `double` 的简单修饰名                                 |
| `PKc` | `const char*`  | `P` = pointer（指针） `K` = const（常量） `c` = char 组合起来就是 **指向常字符的指针** |

## 简单类型示例-

```c++
#include <iostream>
#include <vector>

int main(){
    auto i = 42; // i 被推导为 int
    auto d = 3.14;  // d 被推导为 double
    auto s = "hello";   // s 被推导为 const char*

    std::cout<<typeid(i).name()<<" "<<typeid(d).name()<<" "<<typeid(s).name()<<std::endl;
}
```

- **输出**：

```bash
i d PKc
```

- `auto s = "hello";` 中的 `"hello"` 是字符串字面量，类型为 `const char[6]`，但在 `auto` 推导时会 **退化（decay）** 成 `const char*`。
- 不同编译器或平台输出的修饰名可能不同（例如 MSVC 会输出 `int`、`double`、`char const *`），但  `i d PKc` 明显是 **Itanium C++ ABI** 风格（GCC / Clang 等使用）

### 显示可读的名称

````c++
#include <iostream>
#include <vector>
#include <cxxabi.h>
// 输出：char const*
int main(){
    auto i = 42; // i 被推导为 int
    auto d = 3.14;  // d 被推导为 double
    auto s = "hello";   // s 被推导为 const char*

    std::cout<<typeid(i).name()<<" "<<typeid(d).name()<<" "<<typeid(s).name()<<std::endl;
    std::cout << abi::__cxa_demangle(typeid(i).name(), 0, 0, nullptr)<<std::endl;
    std::cout << abi::__cxa_demangle(typeid(d).name(), 0, 0, nullptr)<<std::endl;
    std::cout << abi::__cxa_demangle(typeid(s).name(), 0, 0, nullptr)<<std::endl;

}
````

输出：

```bash
i d PKc
int
double
char const*
```

## 复杂类型示例-

```c++
#include <iostream>
#include <vector>
#include <map>
#include <cxxabi.h>
// 输出：char const*
int main(){

    std::vector<int> vec = { 1, 2, 3, 4, 5 };
    std::map<int, std::string> myMap = { {1, "one"}, {2, "two"}, {3, "three"} };
    auto it = vec.begin(); // it 被推导为 std::vector<int>::iterator
    auto mit = myMap.begin(); // mit 被推导为 std::map<int,std::string>::iterator
    std::cout << "vec的第一个元素是: " << *it << std::endl;
    std::cout << "myMap的第一个元素是: " << mit->first << " => " << mit->second
<< std::endl;

    std::cout<<typeid(it).name()<<std::endl;
    std::cout<<typeid(mit).name()<<std::endl;
    std::cout<<std::endl;
    std::cout << abi::__cxa_demangle(typeid(it).name(), 0, 0, nullptr)<<std::endl;
    std::cout << abi::__cxa_demangle(typeid(mit).name(), 0, 0, nullptr)<<std::endl;

}
```

- 输出：

```bash
vec的第一个元素是: 1
myMap的第一个元素是: 1 => one
N9__gnu_cxx17__normal_iteratorIPiSt6vectorIiSaIiEEEE
St17_Rb_tree_iteratorISt4pairIKiNSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEEEE 

__gnu_cxx::__normal_iterator<int*, std::vector<int, std::allocator<int> > >
std::_Rb_tree_iterator<std::pair<int const, std::__cxx11::basic_string<char, std::char_traits<char>, std::allocator<char> > > >
```

### 解释见：

https://chat.deepseek.com/share/33z7e9k81wqmtpn1jk

## 返回值类型示例-

```c++
#include <iostream>

auto add(int a, int b) -> decltype(a + b)
{
    return a + b;
}

int main()
{
    auto result = add(3, 4); // result 被推导为 int
    std::cout << "Result: " << result << std::endl;
    return 0;
}
```

- 输出：

```bash
Result: 7
```

# 2. 范围for循环

C++11 引入了范围 for 循环，用于**遍历容器**。

**基本用法：**

​	范围 for 循环使用**语法 for (Declaration: Expression)**，其中

​	**Declaration:**是一个变量声明；

​	**Expression:**是一个表示容器的表达式；

```c++
#include <iostream>
#include <vector>

int main(){
    std::vector<int> vec = {1,2,3,4,5};
    for(int value : vec){
        std::cout<<value<<" ";
    }
    std::cout<<std::endl;
    
    return 0;
}
```

**引用类型，如果容器中的元素较大，或我们需要修改元素，可以使用引用类型：**

```c++
#include <iostream>
#include <vector>

int main(){
    std::vector<int> vec = {1,2,3,4,5};
    for(int& value:vec){
        value *= 2;
    }

    for(const int& value :vec){
        std::cout<<value<<" ";
    }
    std::cout<<std::endl;
    
    return 0;
}
```

**与auto结合使用：**

```c++
#include <iostream>
#include <vector>
#include <map>

int main(){
    std::vector<int> vec = {1,2,3,4,5};
    std::map<int, std::string> myMap = { {1, "one"}, {2, "two"}, {3, "three"} };
    for(int& value:vec){
        value += 1;
    }

    for(const int& value :vec){
        std::cout<<value<<" ";
    }
    std::cout<<std::endl;  

    for (const auto& pair : myMap) {
        std::cout << pair.first << " => " << pair.second << std::endl; // 遍历 myMap
    }

    return 0;
}
```

# 3. Lambda表达式(匿名函数)

**Q3：Lambda表达式可以捕获哪些内容？**

可以捕获值、引用、this指针，还支持默认捕获 [=]、[&] 等。

 [函数对象和lambda函数.md](..\c++_md\3高级面向对象\4函数对象和lambda函数\函数对象和lambda函数.md) 

https://cn.bing.com/search?q=c%2b%2b+lambda%E8%A1%A8%E8%BE%BE%E5%BC%8F&qs=SC&pq=c%2b%2blamb&sk=SC2&sc=12-7&cvid=190C5BFB92804760B8FC90A8295E6176&FORM=QBLH&sp=3&lq=0

Lambda 表达式在 C++11 中引入，为匿名函数提供了简洁的语法。Lambda 表达式可以捕获外部变量，并且可以作为参数传递给函数，尤其在处理 STL 算法时非常有用

**基本语法:**

`[capture](parameters) -> return_type {`
`// function body`
`}`

`[捕获列表]（参数列表）->返回类型，可以省略{`
`函数体`
`}`

- capture（捕获列表）：用于捕获外部变量。
- parameters（参数列表）：函数的参数列表，类似于普通函数的参数列表。
- return_type（返回类型）：函数的返回类型，可以省略，由编译器推导。
- function body（函数体）：实际执行的代码。

```c++
#include <iostream>
#include <vector>
#include <map>

int main(){
    auto add = [](int a,int b) ->int{
        return a+b;
    };
    std::cout<<add(3,4)<<std::endl;

    //捕获外部变量
    int x=10;
    auto print_x = [x](){
        std::cout<<"x="<<x<<std::endl;
    };
    print_x();

    //按引用捕获
    int y = 20;
    auto increment_y = [&y](){
        y++;
    };
    increment_y();
    std::cout<<"y = "<<y<<std::endl;

    //捕获所有外部变量：
    //按值捕获：[=]
    int a = 1,b=2;
    auto add_ab = [=]() {
        return a + b;
    };
    std::cout << "a + b = " << add_ab() << std::endl; // 输出 a + b = 3


    //按引用捕获：[&]
    int c = 3, d = 4;
    auto multiply_cd = [&]() {
        c *= 2;
        d *= 2;
    };
    multiply_cd();
    std::cout << "c = " << c << ", d = " << d << std::endl; // 输出 c = 6, d = 8

    //带有默认参数的 Lambda 表达式：
    auto greet = [](std::string name = "World") {
        std::cout << "Hello, " << name << "!" << std::endl;
    };
    greet(); // 输出 Hello, World!
    greet("Alice"); // 输出 Hello, Alice!
    return 0;
}
```

# 4. 类型别名(using)

**基本语法:**

​	**using alias = existing_type;**

- **`using`**：关键字，表示“定义别名”。
- **`alias`**：你给类型起的新名字（标识符）。
- **`=`**：赋值符号，表示“等价于”。
- **`existing_type`**：已存在的任意合法类型名。

```c++
// 旧式写法（C语言风格）
typedef existing_type alias;

// 新式写法（更直观，尤其处理模板）
using alias = existing_type;
```

`using` 语法在 **定义模板别名（Alias Template）** 时具有 `typedef` 无法替代的优势，因此现代 C++ 推荐使用 `using`。

`using alias = existing_type;` 的唯一目的就是 **给现有类型起一个更短、更清晰或更有意义的新名字**，不创造新类型，不增加运行时开销。

```c++
#include <iostream>
#include <vector>
#include <map>

// 模板类型别名：
template <typename T>
using Vec = std::vector<T>;

int main()
{
    // 简单类型别名：
    using Integer = int;
    Integer x = 10;
    std::cout << x << std::endl; // 输出 10

    // 指针类型别名：
    using IntPtr = int *;
    int a = 5;
    IntPtr p = &a;
    std::cout << *p << std::endl; // 输出 5

    Vec<int> numbers = {1, 2, 3, 4, 5};
    for (const auto &num : numbers)
    {
        std::cout << num << " ";
    }
    std::cout << std::endl; // 输出 1 2 3 4 5

    // 复杂类型别名：
    using StringMap = std::map<std::string, std::string>;
    StringMap myMap = {{"name", "Alice"}, {"age", "30"}};
    for (const auto &[key, value] : myMap)
    {
        std::cout << key << ": " << value << std::endl;
    }

    return 0;
}
```

其中 `[key, value]` 就是 **结构化绑定（Structured Binding）**，它允许你在一行中直接解包 `std::pair`（`map` 的元素类型）的 `first` 和 `second`。

而你在编译命令中**没有显式指定 C++ 标准**，编译器（`g++`）默认使用的是较早的版本（通常是 C++14 或 C++11），在这个默认标准下，结构化绑定语法是**不被支持的**。

> 注意：警告虽然不影响编译（代码可能仍会通过），但运行结果**未定义**，或者程序可能因编译器对语法的理解错误而崩溃。**强烈建议不要忽略此警告**。

启用 C++17 标准（推荐）

```bash
g++ hello.cpp -o hello -std=c++17
```

这样警告会消失，代码可以安全运行。

```bash
PS D:\VSCodeProject> .\hello.exe                                                                
10    
5
1 2 3 4 5
age: 30
name: Alice
```

# 5. 移动语义和 std::move 和右值引用 (&&)

**Q2：为什么需要右值引用？**

为了实现移动语义，减少资源的拷贝，提高性能。

 [5右值引用和移动语义](..\c++_md\3高级面向对象\5右值引用和移动语义) 

提高性能，避免不必要的拷贝。

````c++
std::vector<int> a = std::move(b);
// b的内容被移动到a
````

# 6. 委托构造函数

# 7. 默认和删除的函数 (default 和 delete)

# 8. 列表初始化& 统一初始化语法

```c++
std::vector<int> v{1, 2, 3}; // 初始化列表
int x{5};                    // 强制初始化
```

# 9. constexpr 常量表达式

```c++
constexpr int square(int x) { return x * x; }
```

# 10. 原子操作(std::atomic)

C++11 引入了 `std::atomic` 类，提供了一种无锁机制来实现线程安全的操作。`std::atomic` 通过原子操作避免了数据竞争

**原子变量类型声明：**

`std::atomic<int> counter(0);`

```c++
#include <atomic>
#include <iostream>
#include <thread>
#include <vector>

std::atomic<int> counter(0);
void increment(int n)
{
    for (int i = 0; i < n; ++i)
    {
        ++counter;
    }
}

int main()
{
    const int num_threads = 10;
    const int increments_per_thread = 1000;
    std::vector<std::thread> threads;
    for (int i = 0; i < num_threads; ++i)
    {
        threads.emplace_back(increment, increments_per_thread);
    }
    for (auto &t : threads)
    {
        t.join();
    }
    std::cout << "Final counter value: " << counter << std::endl;
    // 输出 10000
    return 0;
}
```

//输出：Final counter value: 10000

在并发编程中，`std::atomic` 是C++11引入的关键工具，它通过**原子操作**（不可中断的最小操作单元）来避免多线程环境下的数据竞争（Race Condition），是实现无锁（Lock-Free）编程和构建高性能并发程序的基础。

### 🤔 原子操作的概念与原理

原子操作指的是在执行过程中不会被任何其他线程中断或分割的操作。以一个简单的`counter++`为例，它在底层通常包含“读取-修改-写入”三个步骤。在多线程环境中，如果这些步骤不是原子的，就可能导致多个线程同时读取到相同的旧值，各自递增后再写入，最终结果错误。

`std::atomic`则通过**封装基本数据类型或自定义类型**，并利用底层硬件指令（如x86架构的`LOCK`前缀指令）和编译器屏障，来保证对这些封装后变量的特定操作是不可分割的。

### 🛠️ 核心接口与操作

`std::atomic` 模板类位于 `<atomic>` 头文件，其常用接口如下：

- **基本读写**：`load()` 和 `store()` 提供了原子的读取和写入能力。
- **数值运算**：`fetch_add()`, `fetch_sub()` 及重载的 `++`, `--`, `+=`, `-=` 等操作符，可以原子地修改变量值并返回原值。
- **位运算**：`fetch_and()`, `fetch_or()`, `fetch_xor()` 用于执行原子的按位操作。
- **值交换**：`exchange()` 原子地将变量值替换为新值，并返回被替换的旧值。
- **CAS操作**：`compare_exchange_weak` 和 `compare_exchange_strong` 是实现复杂无锁数据结构的基石。

#### 💡 深入CAS（Compare-And-Swap）

CAS操作是许多无锁算法的核心。其逻辑是：比较原子变量的当前值与期望值，如果相等，则将其修改为新值；否则，将当前值加载到期望值中。这个过程是原子的。

cpp

```
int expected = 0;
int desired = 1;
// 如果 flag 当前值等于 expected (0)，则将其设置为 desired (1) 并返回 true
// 否则，将 flag 的当前值写入 expected 并返回 false
bool success = flag.compare_exchange_strong(expected, desired);
```



`compare_exchange_weak` 和 `compare_exchange_strong` 的主要区别在于：

- **`strong`**：保证在值相等时一定成功，但性能可能稍低。
- **`weak`**：允许“伪失败”（Spurious Failure），即在值相等时也可能失败，通常需要配合循环使用，但性能更优。

cpp

```
// 使用 compare_exchange_weak 实现原子递增的伪代码
int expected = counter.load();
do {
    // 尝试将 counter 从 expected 更新为 expected + 1
    // 如果失败，expected 会被自动更新为 counter 的当前值
} while (!counter.compare_exchange_weak(expected, expected + 1));
```



### ⚙️ 内存顺序（Memory Order）

为了平衡性能与正确性，C++提供了多种内存顺序选项，允许程序员在原子操作周围施加不同程度的排序约束。

| 内存顺序               | 描述                                                         | 典型场景                                                    |
| :--------------------- | :----------------------------------------------------------- | :---------------------------------------------------------- |
| `memory_order_relaxed` | **最宽松**，只保证操作的原子性，不提供任何跨线程的同步或顺序保证。 | 简单的、无依赖关系的计数器，只关心最终值是否正确。          |
| `memory_order_acquire` | **获取语义**，用于`load`操作。保证本操作之后的读写指令不会被重排到此操作之前，确保能“看到”其他线程的“发布”。 | 读取一个用于同步的“标志位”，以安全地访问其后被保护的数据。  |
| `memory_order_release` | **释放语义**，用于`store`操作。保证本操作之前的读写指令不会被重排到此操作之后，将当前线程的修改“发布”给其他线程。 | 在写入受保护的数据后，设置一个同步“标志位”。                |
| `memory_order_acq_rel` | **获取-释放语义**，用于`read-modify-write`操作。结合了Acquire和Release的特性。 | 在CAS（比较并交换）循环中，既需要读取当前值又需要写入新值。 |
| `memory_order_seq_cst` | **最严格**，也是**默认**的内存顺序。在所有线程间建立一个全局统一的、顺序一致的操作视图，但性能开销最大。 | 当你对内存顺序不确定，且需要最强的正确性保证时。            |
| `memory_order_consume` | 比Acquire更弱，仅要求依赖于被加载值的后续操作不被重排。**实践中很少使用，且被C++17标记为不鼓励使用**。 | -                                                           |

### ⚠️ 注意事项与常见误区

- **并非所有操作都是原子的**：像 `x = x + 1` 这样的表达式，即使 `x` 是原子类型，其“读取”和“写入”也是分开的，整体不是原子的，应该使用 `fetch_add` 或 `++x`。
- **并非所有类型都支持无锁**：原子操作是否“无锁”取决于类型大小和硬件。可以使用 `var.is_lock_free()` 来检查，对于大尺寸的自定义类型，`std::atomic` 可能会退化为使用内部锁。
- **原子操作也有性能开销**：尽管比互斥锁轻量，但原子操作仍然涉及内存屏障和缓存同步，过度使用或选择过强的内存顺序（如默认的`seq_cst`）可能会成为性能瓶颈。
- **组合操作需谨慎**：单个原子操作是安全的，但组合多个原子操作时，整体逻辑不一定安全。例如，先`load`再`store`，中间状态可能被其他线程修改。此时应使用CAS等操作来确保整体原子性。

### 🔄 与 `std::mutex` 的对比

| 特性           | `std::atomic`                             | `std::mutex`                                      |
| :------------- | :---------------------------------------- | :------------------------------------------------ |
| **粒度**       | 保护单个变量或操作                        | 保护一段代码块（临界区）                          |
| **性能**       | 通常**更高**，利用硬件指令，在用户态完成  | 相对**较低**，涉及系统调用和上下文切换开销        |
| **编程复杂度** | **更高**，需要深入理解内存顺序、CAS循环等 | **相对简单**，使用 `lock_guard` 或 `unique_lock`  |
| **适用场景**   | 计数器、标志位、实现无锁数据结构          | 保护复杂数据结构、文件I/O、需要等待条件变量的场景 |

**选择建议**：当只需对单个变量进行原子操作（如计数、设置标志）且追求极致性能时，优先考虑 `std::atomic`。当需要保护一段复杂的代码逻辑或整个数据结构时，`std::mutex` 是更合适的选择。

### ✨ C++20 的新进展

C++20在原子操作方面也引入了一些新特性：

- **`std::atomic_ref`**：允许对非原子对象进行临时的原子访问，增加了灵活性。
- **原子智能指针**：为 `std::shared_ptr` 提供了 `std::atomic` 的特化版本，使其引用计数的操作本身是原子的。
- **等待与通知**：`wait()`, `notify_one()`, `notify_all()` 等方法使原子对象能够实现高效的线程等待与唤醒机制。

# 11. nullptr

nullptr 是 C++11 引入的一个关键字，用于**表示空指针**。它解**决了传统 NULL 常量****在某些情况下的歧义问题**，因为 **NULL 通常被定义为整数 0**，在重载和模板场景中可能会引发问题。

# 12. 智能指针（RAII 管理资源）

Q4：智能指针和原始指针的区别？

智能指针自动释放资源，防止内存泄露，原始指针需要手动管理。

 [12全局算法](..\c++_md\3高级面向对象\12全局算法) 

```cpp
std::unique_ptr<int> ptr(new int(5));
std::shared_ptr<int> sp = std::make_shared<int>(10);
```

# 13. shared_ptr 底层实现，是否线程安全？

# 14. shared_ptr 赋值给 weak_ptr 时内部做了什么？

# 15. 多线程库（并发支持） std::thread

```c++
std::thread t([](){ std::cout << "Hello Thread"; });
t.join();
```

# 16. 强类型枚举 enum class

 [第4章复合类型.md](..\C++ Primer Plus\第4章复合类型.md) 

```cpp
enum class Color { Red, Green, Blue };
```

# 17. std::chrono
